CREATE TABLE COMPANY (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EMPLOYEE (ID INTEGER NOT NULL, AGE INTEGER, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), ACOMPANY_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE COMPANY_EMPLOYEE (Company_ID INTEGER NOT NULL, employeeList_ID INTEGER NOT NULL, PRIMARY KEY (Company_ID, employeeList_ID))
ALTER TABLE EMPLOYEE ADD CONSTRAINT EMPLOYEEACOMPANYID FOREIGN KEY (ACOMPANY_ID) REFERENCES COMPANY (ID)
ALTER TABLE COMPANY_EMPLOYEE ADD CONSTRAINT CMPNYMPLYEmplyLstD FOREIGN KEY (employeeList_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE COMPANY_EMPLOYEE ADD CONSTRAINT CMPNYMPLOYEECmpnyD FOREIGN KEY (Company_ID) REFERENCES COMPANY (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_TABLE', 0)
